import subprocess
from setuptools import setup, find_packages, Extension

setup(
      name='gpusim_fdw',
      version='0.0.1',
      author='Pat Lorton',
      license='BSD 3-Clause New',
      packages=['gpusim_fdw']
)
